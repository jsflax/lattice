# First tick and bounded manual drain, version 2

Unlaunched source packet. Copies the historical retention foreground harness at SDK289ed743; ORIGINAL-FILES.json binds unchanged predecessor bytes. Product Core is never patched. This version keeps fixture/writer/retention/busy-timeout/first-tick timing/held-reader/final-data oracles and existing per-command budgets. The old single-tick all10,000-deleted assertion and claim-barrier experiment remain unchanged in their historical packet.

New protocol: maintenance reports the first tick, then waits until the supervisor reaps the foreground writer. Only then does it query residual old IDs. Baseline must have0; bounded must have7952, then5904/3856/1808/0 after at most4 further public manual calls. Recent-claim A controls remain10000. Every continuation and post-call residual is separate. Final validation still requires original1010 audit rows,110 live models, exact10 final values and sequence11010. A held reader stays pinned through the entire extension and must still see10010 rows and block truncation until released.

The old in-SQL two-writer claim barrier is excluded from this new performance schedule because owned claim setup would deadlock that rendezvous. Native claim/floor correctness tests and any future before-setup diagnostic remain separate. Existing floor/rollback/late-remote correctness control is retained as a prerequisite, subject to source review and actual execution.

The candidate SOURCE-CONFIG binds isolated beee over d546. A baseline packet must bind d546 separately. There is no hosted installer, aggregate source schedule or run admission yet; HARNESS-SOURCE/SOURCE-MANIFEST are not sealed until source review. No native measurement has run. test_protocol.py uses synthetic Python processes and bytes, never SQLite.

Report every foreground interval, p50/p95/p99/max, top10 stalls, A/A spread and B effects; first tick, each continuation, sum of public call times and entry-to-final-observation separately. Filesystem main/WAL/SHM samples are lower bounds and absent windows are null. Their foreground/continuation/total envelopes use only supervisor monotonic timestamps and include signal/pipe/receipt latency; Python timestamps are never compared with C++ steady_clock epochs. Native write/tick durations and overlap remain in the original native clock domain. No automatic100ms scheduler, full shutdown, trace transaction duration or2× claim follows.
