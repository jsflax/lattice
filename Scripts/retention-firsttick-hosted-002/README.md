This is a held source-only hosted package for the accepted version 2 retention first-tick/manual-drain protocol. It compares exact baseline d5463886856cd93391cf7dd088910a59cfc3b2f1/tree1f8a2fd6dfb65f5fc81b2c7803f3a45fe107fe28 with isolated bounded-retention beee2f94838ff196eaaedefcffa3c0fb5f70e6b6/treee226c905e4da55fe81a0c5da78ed062f5f8284fa. Product sources are exported from authenticated Git objects; no overlay changes them.

The existing registered retention-foreground.yml filename is reused on a future qualification branch. The template is manual-only and its job is disabled. Root must separately approve the exact packet, final workflow installation/enabling, qualification commit and one attempt-1 dispatch. CONFIG.executionAdmitted=false describes this held preparation; only the exact external root admission checked by hosted_guard.context can admit the later invocation. No run is admitted by this packet or its pure tests.

The accepted protocol, its parent/independent review, historical source hashes and earlier proposal are preserved under evidence. Its probe.cpp, supervise.py, validate.py, prepare.py, CMakeLists.txt and guarded_runner.py remain byte-identical. The original run.py body is unchanged; its only adaptation selects AggregateRunner and brackets each direct stage in Stage. RUN-ADAPTATION.diff records that small delta. Original retention source configurations and both complete 883-file manifests are byte-identical to the parent preparation.

One Ubuntu 24.04 x86_64 job executes the fixed sequence: toolchain installation; baseline fetch, prepare, build, run; bounded fetch, prepare, build, run; final binding of both results. Each arm keeps one warmup, five measured alternating A1/A2/B triplets, both held-reader controls, native correctness prerequisites and every survivor/value/sequence/flag/checkpoint oracle. There is no added native workload, trace callback, retry, automatic scheduler measurement or unreviewed product change. Historical single-tick and claim-barrier failures remain separate.

The workflow invokes each arm's existing harness/run.py directly. No outer process supervisor launches these stage runners. AggregateRunner is a small in-process subclass of the original GuardedRunner, which still owns the actual compiler or sample-supervisor process group. The unchanged sample supervisor launches its children in that group with start_new_session=False. This preserves the original observed group cleanup scope; it does not claim cgroup containment or universal custody of arbitrary process-session escapes.

The clock is captured before fetching workflow tools into a fresh $HOME/localdev/lattice-retention-firsttick-<run>-1 root. BOOTSTRAP binds the monotonic start, host boot ID, root, repository, workflow commit, run ID and attempt. Every stage validates the same bootstrap, reviewed seal and exact admission bytes. Global metadata survives stage-process exit; a fresh stage cannot reset the deadline. The 240-minute aggregate envelope includes tools bootstrap, package installation, source transfer and both arms. Five minutes are reserved for artifact upload, so native/control stage cleanup cannot pass start+235 minutes; the original 30-second cleanup reserve stays inside that cutoff. The workflow also has a 240-minute job limit. Host cancellation can still destroy evidence; the reserve is not a claim of surviving arbitrary GitHub termination.

Each original per-source 512 MiB packet cap, 32 MiB command log cap and 12.5 GiB raw-free floor remains. Every active child sample also measures all owned task-root files against an independent 1.5 GiB aggregate cap, including tools Git objects, both source packets/builds/logs, fetched Core objects and control receipts. The completed other arm's 512 MiB bound is checked too. This is sampled disk accounting using allocated blocks, as in the original guard, not a hard write quota/RSS/CPU governor. The initial tools transfer is bounded by the workflow's 5-minute bootstrap and is checked against disk bounds before any package/compiler/native command; no running Python guard is claimed before its own code has been fetched. Package-manager system-managed installation state is outside the owned-artifact byte sum, while its logs/time and global raw-free floor remain checked.

Original stage limits are retained: prepare/build 1500 seconds, run 3600; compiler command 1200; regular sample outer 120 and supervisor 115; one compiler job and uniform O3. New source-fetch stages have 150 seconds and individual git timeouts 15/90/15. Tool installation keeps a 300-second stage. AggregateRunner clamps each original stage work/cleanup deadline downward to the common cutoff and requires a full unchanged command timeout before launch. Insufficient remaining time is refusal, never a shorter measurement or a timeout increase. New source/fetch/setup work consumes this envelope rather than extending it.

Every stage exclusively writes START and END receipts. A successor requires exactly the completed successful prefix, no missing or failed stage, unchanged predecessor command receipts/logs and no duplicate intent. Finalization independently checks command cleanup and logs, source/manifest/tool/admission bytes and resource/deadline state. It keeps the first exception separate from later evidence failures. Successful fixtures are removed only at the original proven cleanup points; failed fixtures, command logs and partial state remain. The artifact step uploads both whole bounded arm directories, protocol/tools packet, admission/context and hosted/control receipts. A failed/incomplete stage cannot start the next arm or be silently rerun.

The host's explicit clang-18, clang++-18, SQLite header and system library bytes/resolved paths are recorded after installation and rechecked across stages. The original native version/build/object/archive/link-map proof and each probe's SQLite runtime identity remain in force. This selected identity check is not a complete independent archive of every compiler/SDK/system-library input.

The result preserves independent per-arm ANALYSIS and RESULT records. COMPARISON-INPUTS binds their hashes and both exact sources; successful finalize-END is also required before claiming complete collection. First tick, each manual continuation, total public-call time, entry-to-final-observation, all 1000 foreground intervals, tail stalls, A/A spread, held-reader and filesystem envelopes remain separately reported by the accepted protocol. Fixed sequential source order does not remove time/thermal/cache effects or establish physical idle-host identity. No p95 stability, automatic 100 ms worker pacing, lock-cause attribution, release benefit or frozen 2x target follows without the later evidence assessment.

Source checks and test_hosted.py use only byte/JSON/AST checks, synthetic clocks/files and mocked command/process interfaces. No local compiler, native/SQLite invocation, resolver, dispatch or push ran while preparing this package. Final independent/root review and actual hosted input/build/runtime/cleanup qualification remain open.
