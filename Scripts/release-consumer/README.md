# Published SDK2.0.0 / Core2.0.7 consumer and iOS qualification preparation

Publication is unbound and runtime admission is disabled. Candidate Core tree ff0bff48d606849cb8787d665866ff1b02bb9fa1 is the source expectation; it is not a published tag or release receipt. The actual public commit/release/run/receipt must be authenticated and sealed on a separate reviewed successor. Historical Core2.0.6 release files are excluded from this active packet; all SDK2.0.0 evidence and34pins remain exact.

The existing finite macOS consumer must resolve published versions, write three exact records, roll back a fourth and verify persistence in a separate read process. The iOS leg builds arm64 simulator code with actual dependency/compiler-input checks; it does not claim simulator/device execution. Source checks do not qualify either runtime gate. Commands, budgets, cleanup and those oracles remain unchanged. No release, native execution, dispatch or dependency adoption follows from this preparation.
