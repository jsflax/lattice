# Published SDK 2.0.0 / Core 2.0.7 consumer and iOS qualification packet

Core 2.0.7 publication is bound to public commit `eb9a99c5d3945108ab9d9d0d39622616d82f3586` and reviewed tree `ff0bff48d606849cb8787d665866ff1b02bb9fa1`, stable release `393308437`, and successful release workflow `35656538973`, attempt 1. The packet retains the actual release, public tag/commit, run metadata and 384-byte receipt asset unchanged. The receipt SHA-256 is `f45d9ec814c6a230cfc05cd54d32874e96046cc98f694e370bb7971c8668f100`. The sealed independent publication assessment accepts this source publication and keeps public consumer and iOS adoption pending.

The publication validator now admits this bound packet. Dispatch readiness describes its inputs; exact-source review and root dispatch remain separate. This local preparation has not been pushed or dispatched and claims no consumer/iOS runtime result. GitHub reports the release is mutable, so saved publication identities do not promise future immutability; both unchanged qualifiers reauthenticate the actual dependency sources when run.

All 216 released SDK files and all 34 SDK pins remain exact, including the SDK's original Core 2.0.4 pin. Only the consumer root's existing explicit exact Core 2.0.7 requirement advances the effective consumer graph. The historical SDK upstream-binding receipt remains unchanged as evidence of that original SDK release.

The existing finite macOS consumer must resolve published versions, write three exact records, roll back a fourth and verify persistence in a separate read process. The iOS leg builds arm64 simulator code with actual dependency/compiler-input checks; it does not claim simulator/device execution. Commands, budgets, cleanup and those oracles remain unchanged. Pure JSON checks qualify the binding only. The separate WASM gate and all inherited qualification limits remain independent.
