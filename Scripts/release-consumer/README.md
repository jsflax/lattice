# Published external consumer 003

This successor binds unchanged consumer002 to actual published SDK2.0.0 commit `0461b210aa028f3c4bb6165fed895e0f4385b565` / tree `a1faa2850ebf2bf7dca152feffa461a24755a154`, and Core2.0.4 commit `99e4bc89389d14072e6ab9b658db49a49a3573f5` / tree `cbe72711a69fe71451b2398253b5895ebad5bda4`. Parent completed SDK publication authentication SHA `489a6cb6f3c5f9036d63d9b4e60aea045eceb55f31b3785affe7e6eabb511587`, including required Linux/macOS/iOS release gates and the informational nightly failure. Its exact receipt is retained here. Actual SDK release run35402910258 attempt1 and Core release run35397616267 attempt1 are bound through raw API JSON, not main/PR CI records.

`publicationQualified` and `dispatchReady` are now true for this reviewed-input experiment. **The external consumer has not been resolved, compiled, or run.** Runtime acceptance and downstream adoption remain false/unqualified until actual evidence exists. Parent owns installation/dispatch after independent packet review. No source modification, network access, native/compiler/Swift/SQLite execution occurred during this preparation; only Python validation checks ran.

## Preserved experiment

`qualify.py`, `validate.py`, `guarded_runner.py`, `config.json`, and all three Swift consumer files are byte-identical to002. The guard remains `c31626b5a02692663d5c28bacecc61db474eee640a9d2c8bf947b0e68fd41b71`. `OBSERVED-SDK-PINS.json` remains the historical cc24/Core2.0.3 reference; all33 non-Core complete pin objects equal the actual published SDK's34-pin lock. No development graph, source overlay, mirror, or dependency substitution is introduced.

The only Python source delta is one synthetic-test setup line explicitly resetting publication/dispatch gates to false. The original unbound-refusal test remains meaningful when the packet itself is bound; `bound()` still turns those synthetic gates true. All18 original test methods/assertions are retained. `TEST-FIXTURE.patch` records that isolated change; it cannot change the runtime adapter.

The executable uses the public Model macro, writes three fixed UUID rows, verifies a deliberate fourth-row transaction rollback, closes, then reopens in a separate reader process and checks exact Int/String/Double values. Successful output followed by process failure cannot pass. The same hashed executable must pass both phases, with complete command receipt/log/cleanup checks. Store files/sidecars remain retained, including failures. Scope is macOS public consumer behavior only: no iOS runtime, sync, attached-store, performance, or downstream-installation claim.

The adapter authenticates both fetched tags and complete tracked SDK/Core source; full committed SDK34 pins seed an external consumer lock plus the SDK pin. Actual SwiftPM resolution must produce its own originHash and exact allowed pin objects. It records any product-selected omissions, checks graph/workspace/lock equality and clean exact checkouts, and repeats source/lock/binary/receipt/resource/deadline checks at acceptance. This is ordinary fresh-build provenance, not exhaustive compiler-object/system-header proof. Missing/drifted evidence clears all acceptance flags.

## Bound inputs and installation

The six fixed files in `publication/` are the actual SDK/Core release objects, successful release-run records, Core release receipt and combined upstream binding receipt. `publication-inputs.json` holds their full hashes, tag identities, exact published SDK lock/binding hashes and complete34 pin objects. `PUBLICATION-AUTHENTICATION.json` retains the parent's platform/source/publication acceptance. `PREPARATION-PENDING.json` is historical evidence that this successor began with both execution gates false before the completed parent receipt arrived; it is not the final gate.

The new input seal covers all runtime inputs, publication evidence and retained source/check artifacts. Workflow and SOURCE-READY remain outside that input seal to avoid a circular hash. The workflow derives from independently reviewed activation001: path-filtered push on `codex/perf-qualification-20260918` for `.github/workflows/release-consumer.yml` or `Scripts/release-consumer/**`, alongside manual dispatch. Its only delta from that reviewed workflow is the new packet-seal literal. Install the bound subtree at `Scripts/release-consumer` and the workflow at `.github/workflows/release-consumer.yml` together, after accepting SOURCE-READY. Pushing that branch can immediately start qualification; do not install an unbound intermediate. The workflow records/fetches exact GITHUB_SHA/tree before invoking the adapter.

All bounds and commands are unchanged: fresh owned `~/localdev/lattice-release-consumer-<run>-<attempt>/qualification`; macOS26 arm64, installed Swift tools6.3 or newer; resolve600s, Debug build5400s with-j2, separate writer30s/reader30s, overall7200s/reserve600s;12GiB free floor,30GiB total packet,512MiB build/resolve logs and1MiB runtime logs. Workflow130min, guarded step121min. All explicit scratch/cache/config/security/module-cache/temp/fixture paths remain under the task root; HOME is unchanged. No unchanged retry or automatic limit relaxation.

The installed CLI remains `python3 -B "$TASK_ROOT/tools/Scripts/release-consumer/qualify.py" --root "$TASK_ROOT/qualification" --packet-seal-sha256 "$PACKET_SEAL_SHA256"`. Artifacts retain sources, generated lock, receipts/logs and fixture, while omitting large scratch/checkouts and binary. Hosted before/after hashes establish the documented custody; an unuploaded binary is not later independently rehashed locally.

Original002 remains unchanged and unbound at seal `1f9cd09502b147a58bbf2a11d2b980d2fdeceb677908119a53291995da1e2930`. The source-ready receipt records real bound-input checks and18 synthetic rejection checks separately from unrun native acceptance.
