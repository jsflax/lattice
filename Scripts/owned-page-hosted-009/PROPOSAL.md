# Hosted009: exact owned-page SDK qualification

Source preparation is feasible and implemented as a held draft. SDK008 remains byte-exact and unlaunched; hosted009 is a new invocation with a new source seal, root admission and runtime root. No compiler, package resolver, database, native test or hosted dispatch has run during this preparation.

## What this closes

The target is the real SDK070 graph with private Core919, FRT1 and the owned-read feature enabled uniformly: the original 12 Swift Testing declarations/26 fake-source invocations plus the separate nine direct Swift/C++ owner cases. It is a correctness/importer/lifetime qualification, not a latency experiment, public activation, FRT0/iOS qualification or acceptance of the asynchronous physical C++ backend.

The current full SDK2d749 suite does not replace it. `CURRENT-SDK-COVERAGE.json` binds SDK2d749/tree7d835d1 and the source search. Its `ProjectionReadExecutor.swift` and nine executor test bodies are byte-identical to SDK070: they overlap on generic queue/cancellation/deadline/cleanup prerequisites. However, SDK2d749 has no `OwnedPageRead`, `OwnedPageByteSource` or `LATTICE_EXPERIMENTAL_OWNED_READ` occurrences across Sources, Tests, Package.swift, Scripts and workflows. It has neither the owned audit-page decoder nor the 26 adapter invocations or the nine direct runtime cases. Its two exact-vector bridge tests return live model rows and do not test owned audit-page backing lifetime, raw audit bytes, release counters or operation-stop aliases. This is a source coverage assessment, not a claim that a current test run passed.

## Fixed inputs and relocation

- SDK commit `0706257cbaf0b23e0f8ebeeba34ec4f7b234f030`, tree `12b932471c32b513d7a13fa13bc0b2ea943393a2`, full 256-file manifest.
- Core base commit `a09e622e4da3f603698d9f1069d6118e79397b2e`, tree `b77b51371ff107a81a05a4d5c259683f65120f4b`, 901 files; exactly 29 manifest-bound postimages reconstruct the original 919-file private source. There are no removals and no new product edits. This is a composition, not an invented Core919 commit.
- The original 34 Package.resolved pins, six qualification-overlay files, expected test inventory, fake-source oracle, direct-runtime oracle and compiler/link proof are unchanged. Both before/after effective graphs still authenticate all 34 identities; only the exact staged Core is edited.
- The 12 original compiler/test command records are normalized only at the original runtime-root prefix. Native Debug build, actual driver jobs 2, uniform FRT1/feature1 flags, discovery, no-parallel 26-case filter, direct runtime and system-SQLite linkage requirements remain exact. Twelve new source-transfer commands authenticate SDK/Core Git HEAD, tree, clean checkout and full manifests before overlay staging.

A new `$HOME/localdev/lattice-owned-page-hosted-<run-id>-1` contains the exact workflow tools checkout, `run`, its sibling `run-owner`, bootstrap receipts and temporary files. Runtime/config/cache/module-cache/data paths are rendered from a sealed `@RUNTIME@` template. No historical `/Users/jason/...` path is executed or followed on the hosted runner. SDK008's original seal and all 41 sealed files are retained beneath `evidence/sdk008` as immutable data; the new source validator authenticates them and their independent review but does not import/replay SDK008's recursive historical source checker.

Root's `REMOTE-SOURCE-AVAILABILITY.json` records the Core base object/tree availability and initial SDK070 404. `SOURCE-PUSH.json` and `SOURCE-REMOTE-AFTER-PUSH.txt` record root's later narrow SDK070 source-branch publication. Actual hosted fetch plus HEAD/tree/full-manifest validation remains mandatory; a branch tip alone is not source acceptance.

## Hosted tool identity is a new binding

The held macos-26 workflow requires arm64 and the declared `/Applications/Xcode.app` tool/SDK paths. It records the actual hosted SHA256 and resolved path for swift, swiftc, clang++, CxxStdlib interface, SDKSettings and SQLite stub, then rechecks these through qualification. Actual compile/link commands and products are joined by the unchanged SDK008 parser; a new compiler output shape fails closed.

The packet does not assert that hosted Xcode matches the original local Xcode hashes. Admission must explicitly accept this hosted identity policy. `HOSTED-CONTEXT.json` binds those initial tools, exact workflow SHA, run ID, attempt 1, packet seal and original admission bytes; its hash is retained by the owner and result. Selected tool hashes are not a claim that every SDK/toolchain byte is independently archived. The existing compiler dependency proofs retain their original scope.

## Lifecycle and resource bounds

`guarded_runner.py`, `process_custody.py` and `detached_owner.py` are reused without byte changes. They retain OS birth identity plus observed ancestry, individually authenticated signals, rejection of ambiguous runtime-path candidates, durable command argv/cwd and owner/result receipts. The new wrapper only launches that owner once, waits in the same GitHub step until its terminal receipt and actual owner exit, and translates wrapper interruption into its exact authenticated STOP file. It does not restart, install a service, signal by process name or claim universal descendant discovery.

All SDK008 native limits remain unchanged: two jobs; 12 GiB free floor; 8 GiB packet; 512 MiB command-plus-owner log cap; 9,000 seconds total from owner launch including remote source fetch; 600-second final reserve; resolve 600; build 5,400; focused tests 300; direct runtime 30. Each command must still fit its full unchanged timeout within remaining work time. New hosted byte accounting includes the tools checkout, source transfers, owner output and all run files once under the same 8 GiB ceiling. Limits are sampled, as in SDK008; a crossing is a failed attempt, not permission to raise them.

The surrounding workflow has a separately bounded 10-minute tools/admission bootstrap and 15-minute artifact upload, with a 180-minute job envelope. These are transfer/evidence overheads, not extra native work time. The workflow stays open while the detached owner runs. GitHub cancellation or host-wide termination can still destroy evidence or processes outside observable cleanup; no source test promises survival of undocumented host termination. Observed owner/descendant closure and terminal evidence are required for success; a missing owner/terminal or ambiguity cannot trigger a retry. Automatic attempt 2 is refused. Admission is exact for one workflow SHA/attempt 1; run ID is assigned by GitHub and bound at start. The dispatcher must issue the one authorized dispatch; no cross-run nonce service is introduced.

## Concrete preparation and remaining steps

The draft contains the held workflow template, `hosted.py` admission/owner wait, `hosted_binding.py` exact source/tool/path joins, the small `qualify.py` adaptation, original proof/oracle/lifecycle sources, 29 Core postimages and source manifests. The wrapper uses the hosted exact-Git-fetch/localdev/artifact pattern already exercised by profile003; source-transfer commands run through the stronger SDK008 descendant guard.

79 pure tests pass in `PURE-CHECKS-003.log`: 65 retained case identities with only source/relocation test adjustments, plus 14 hosted cases. They cover exact admission/refusal, attempted rerun, path-only command relocation, Core manifest composition, remote HEAD refusal before postimages, actual hosted-entry ordering before staging, bound terminal/result, owner exit, authenticated stop and deadline failure. The initial assertion mistakenly prohibited the local test temporary root; that failure and correction remain in `PURE-CHECKS-initial.log` and `PURE-CHECKS-002.log`. No fresh OS lifecycle smoke or native result is claimed; SDK008's original lifecycle acceptance is retained separately.

Before execution: independently review the hosted adaptation and exact packet bytes; root approves the new hosted allocation/tool identity; install these reviewed packet files under `Scripts/owned-page-hosted-009` in an isolated qualification branch; install/enable the separately reviewed workflow; commit and bind its exact workflow SHA and packet seal; perform one explicit dispatch with exact admission JSON. Installation/enabling changes require their own final source binding. Do not edit SDK008, repin SDK070 to current SDK2d749 or silently loosen a parser after failure. On completion, retain complete bounded `run`, `run-owner`, tools packet and bootstrap/context/result evidence, then independently assess discovery, 26+9 outcomes, compiler graph, pins and cleanup before claiming qualification.
