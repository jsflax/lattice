# Proof lineage

build_proof.original.py preserves the original Release proof. build_proof.004.original.py preserves the prior Debug two-product proof. The current build_proof.py and its added parser tests must equal the separately sealed owned-page-build-proof-005 packet exactly. DEBUG-PROOF.patch records cumulative proof differences; SUCCESSOR.patch records005→006 owned-path and stricter packet-cap metadata changes; the proof itself is unchanged.

The only new grammar handles the actual retained DocC typed-state direct compiler rows and consumed rpath loader operand. Unknown driver/source/response forms still refuse; ordinary response expansion precedes classification and jobs checks, and no plugin compiler is exempt from explicit jobs2. Output/source/state hashes are retained for final custody. This grammar correction is separate from the test-only Swift call correction.

The unchanged execution wrapper demands native Debug objects and exact layout; actual Lattice testability; four required Swift modules; source/object/map/response/link joins into distinct test and direct executables; and no unrelated test/GTest objects in the direct runtime. Temporary derived child linker-list bytes retain the previous limited provenance scope. Actual native/importer flags and compiler binary hashes remain required. Nothing compiled in the failed004 run is reused or accepted here.

SwiftPM inputHash remains an opaque state field; it is not independently recomputed. Plugin custody authenticates the typed command, exact source set and retained compiler/state/output bytes, without claiming a complete ephemeral plugin object/link graph.
