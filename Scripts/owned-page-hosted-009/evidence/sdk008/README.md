# SDK008: detached ownership and complete descendant cleanup

HELD for independent source review and a new native resource allocation. SDK006 remains an interrupted failure; its outputs are never reused. This packet keeps SDK070/Core919, all dependency pins, the 26 fake-source invocations, nine direct cases, Debug compiler commands, jobs 2 and every SDK006 resource/time limit unchanged. Only owned paths and lifecycle custody change.

Run the native qualification only through `detached_owner.py --reviewed-sdk-owned-page-qualification --source-ready-sha256 <reviewed SHA256 of SOURCE-READY.json>`. Direct `qualify.py` execution refuses a missing authenticated owner. The entry point creates an exclusive sibling `-owner` directory and a detached, reparented qualifier, then returns within five seconds after an OS-identity acknowledgement. Existing intent/root or uncertainty never triggers another launch. No scheduler, service, automatic retry or restart is installed.

`process_custody.py` uses macOS `proc_pidinfo` birth seconds/microseconds, observed parentage and retained identities across Swift process groups. It never reads a process environment. Runtime-path matches without authenticated ancestry are refusal evidence, never signal authority. Such an orphan may require separate recovery; it cannot pass as complete cleanup. TERM/KILL target individually revalidated owned PIDs, never a broad compiler name/group. Normal command acceptance requires original-group absence, leader reaping, all observed owned identities gone, no ambiguous runtime process and no cleanup signals/errors.

The owner directory contains immutable intent, owner and terminal receipts, bounded output, and a replaceable small status record. Authoritative liveness still requires matching the actual OS PID/birth identity; an old file is not liveness. To request a terminal stop, create `STOP.json` atomically containing exactly the owner receipt's `nonce` and `identity`. This stops the current command and preserves its first failure. The overall clock starts before detachment; owner output and evidence count toward the unchanged 8 GiB packet cap and 512 MiB log cap.

The Python-only smoke terminates the launcher after acknowledgement, verifies the detached owner continues, tracks three synthetic descendants in three groups, requests STOP, checks complete closure and checks an unrelated control process survives. The two SDK007 smokes passed in a combined 9.41 seconds and remain retained. Independent review then found the SDK007 prelaunch-owner check was incorrectly repeated inside the qualifier. SDK008 separates those phases, adds pure tests for actual entry ordering, and adds bounded durable command argv/cwd, owner executable/argv and terminal result hashing. A fresh SDK008 smoke will bind these final lifecycle bytes before native admission. No smoke runs a compiler, package resolver, database or network. Neither smoke grants SDK qualification.

`SOURCE-DELTA.json` and `SUCCESSOR.patch` describe changes from sealed SDK006. `FAILED006-AUDIT.json` preserves the interruption assessment. Historical `PROOF-REVIEW.md`, build proof corrections, overlay and original manifests remain byte-exact evidence from SDK006; they do not assert the new lifecycle code was previously reviewed.

Discovery is sampled. Closure proves absence of observed owned birth identities and no ambiguous exact-runtime-path candidates; it cannot prove discovery of a child that forks/reparents entirely between snapshots and omits the runtime path. Such uncertainty is not permission for broader signals. The smoke exercises POSIX launcher termination, not an undocumented host-wide force-kill mechanism.
