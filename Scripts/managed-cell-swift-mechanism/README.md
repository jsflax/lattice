# Actual Swift managed-cell mechanism — source-only successor005

This implements the parent-approved proposal001 without modifying that proposal, the active SDK/Core checkouts or any prior evidence. It is prepared for review, not admitted or run. Source bases are SDK af7775a269d87c37f2ad9a25a5aa60bbcac7b88f/tree ebce11cee9cac7d62e08c0ff830703d66c9c78d2 and Core234afb4ad4f74174bd875035e6bb47b1a878ac56/tree06cacd09ecaf80a460418dad52c352abade58654 plus the exact source001 statement-reuse prototype already qualified for15 narrow C++ cases.

The source-only `checks/composition/` copies contain905 Core files and252 SDK files (with the declared edit link separately authenticated). Core adds the prototype's ten-path delta and a four-path counter overlay (two overlapping headers, two new private files). SDK changes only the frozen benchmark file with seven labeled diagnostic blocks. Stripping those blocks exactly reproduces frozen benchmark SHA993d3a6f384958185c9ee5817e6ba1f125a037f722a074a3a719ef71226455e2. The original scalar expressions, order, `measure` function, page/timer scopes, six-field values, identity assertions, fixture creation, sample policy, Update11 and full-store postimages remain exact.

## Concrete execution shape

Install `packet/` as `Scripts/managed-cell-swift-mechanism` and the separate workflow into `.github/workflows/managed-cell-swift-mechanism.yml`, only after root review. Dispatch manually with the exact installed PACKET-SEAL SHA256. The workflow fetches its own exact GITHUB_SHA under a fresh localdev root and preserves workflow source/run identity. It selects macos-26, records the actual Swift/SDK, and invokes one thin adapter. It does not publish or integrate the prototype.

`qualify.py` reuses unchanged GuardedRunner c31626b5… and unchanged qualified attached-runner006 `build_proof.py` d527ba9d…; it does not introduce another process supervisor. Build-proof reuse preserves its canonical stable linker-list/transitive object/archive/bundle evidence and explicit lack of independent ephemeral child-list bytes. Actual verbose C++ actions must all contain both diagnostic definitions. Every observed Swift C++-interop compiler-driver action must forward both definitions through `-Xcc`; Lattice and LatticeTests must also be present, with Swift's diagnostic condition. Driver arguments are actual emitted compile actions joined to objects/binary, not direct capture of each integrated frontend child. Conflicting or missing flags reject. No flags are added to default manifests.

One fresh Release `swift test -j2` build retains testability; the benchmark opt-in is disabled while building. One subsequent `--skip-build` process runs the original local variant then attached variant, on the same authenticated diagnostic image, each with five warmups and100 measured iterations. Selected-batch writes are absent; the existing legacy Update11 branch stays identical throughout. No A/A2, OFF build, latency comparison or general regression suite is added. The current task requests mechanism evidence only.

Bounds:5,400s build,1,200s workload,9,000s whole adapter with600s final reserve;12GiB free floor,30GiB owned-packet ceiling,512MiB command-log cap and0.5s sampling. The unchanged60s settle is retained without claiming an idle host. Full command timeouts must fit before admission. Previous full Release benchmark build guard receipts took1,680.45s/2,847.31s; these inform the cap, not a promise for the new graph. The smaller512MiB C++ packet guard is unsuitable for a complete Swift dependency build.

All source/scratch/cache/config/security/modules/TMP/logs/fixtures are owned below the new localdev root. Resolve preserves all34 initial SDK pins, then declares Core as the sole exact edit override. Non-Core identities/revisions/URLs and actual workspace paths are checked before and after. Full effective source hashes, build proof, binary/bundle, linked archives/objects and response files are reverified after execution. Full per-command receipts are read from disk rather than treating GuardedRunner.records summaries as receipts. Any command, cleanup, source, graph, binary, result, resource or clock failure prevents final acceptance. Outer workflow/adapter exit0 and RESULT success are both required; a deadline or write failure after emitting RESULT cannot be treated as accepted from that file alone.

## Counter and value evidence

The diagnostic method is a private, default-off `swift_lattice_ref` member: it can reach its private impl() without exposing an object/SQLite pointer. A distinct friend avoids collision with the existing GTest friend definition. It only reads scalar state and copies pool.inspect() under the pool mutex; it performs no SQL, invalidation, callbacks or raw-handle access. The retained synchronous query owner must not be concurrently closed/reopened/moved. This is not a new generic writer-lease API.

Four POD snapshots bracket cold/warm scalar timers; no per-getter hook is installed. The cold pair remains inside the unchanged enclosing read.total, so its extra overhead invalidates any frozen performance-total claim. Sidecar conversion/reflection/serialization happens after the original warm value and identity assertions, outside all timers. Reflection counts the already-read value struct's six stored fields without triggering any model getters. Two records per iteration join the original row count,600-SQL phase count and value checksum. Exactly210 sidecars/420 phase records are required; each is at most4,096bytes and the total at most1MiB. Warmups remain labeled and preserved. All252,000 scalar executions are accounted for.

Pool counters are connection aggregates while SQL is thread-local. Equality alone is not generic per-thread attribution. Here, the selected synchronous benchmark alone owns the displayed `PerfRefinementMemory` references; it creates no model-reader Task or observer/network subscription and does not hand these objects to a worker. The fixed scalar expression is the only producer of managed getters on that owner during these intervals. Source inventory confines query_managed_cell callers to managed<int64_t>/double/string detach; Core's maintenance/notification SQL funnels do not invoke this fixture's Swift model fields. Generic/background SQL and retirement remain enabled, so their effects are retained. This fixture-specific source contract is required for interpretation; counters must not be reused to assert exclusive attribution for an arbitrary application.

The validator requires complete original results and all original invariants; exactly600 executions and `delta(hits)+delta(prepares)==600`; usable, non-escaped/non-closed/non-disabled endpoints; active/suspension zero; retained bounds and no reset failure. It rejects no-hit and bypassed intervals. Expected clean signatures are6prepares/594hits locally,12/588 attached, and0/600 warm. Extra prepares/retirement are preserved and make `cleanExpectedSignatures=false`; they are not normalized away. Observed reuse can still be useful with a non-clean signature. `mechanismQualified` means only the bounded observed reuse/value/admission gate plus custody succeeded. Performance, release, transparent/default-enable and full-plan claims remain false.

Unknown external auto-extension authorizers remain unsupported for transparent enablement. The opted-in image explicitly assumes the known stock runtime plus existing trusted module setup; this experiment does not discover, replace or waive unknown callbacks. Public `database::handle()` still permanently disables/purges reuse. No Core policy/generation/lease code from the qualified prototype is changed.

## Preparation checks and remaining gates

Pure Python checks cover exact finite joins, Optional/bool counter rejection, raw/closed/suspended states, pool bounds, mismatched values/fields/SQL, missing/duplicate inventory, missing/conflicting compiler/importer flags and final acceptance clearing. They are not Swift/native tests. Root's prior15 C++ cases remain separately scoped. Swift import/typecheck, compilation, actual runtime sidecars, toolchain argument shape and performance remain pending.

One preparation-only transport defect was retained in COMPOSITION-ATTEMPT-001: the prior composite prototype diff lacked Git new-file-mode metadata, so forward apply reported dev/null missing. PROTOTYPE.patch remains exact and immutable; PROTOTYPE-APPLICABLE.patch adds only three `new file mode100644` lines. Complete905-file postimage equality independently proves no implementation change. Every patch now applies cleanly offline to the exact archived bases. No compiler, native workload, database, network, commit or push ran during this preparation.

## Narrow parser successor003

The sealed002 packet remains unchanged. History found that it still copied the older runner005 parser, which rejects the known threaded WMO output-map shape when the empty-key global object is intentionally absent. This successor copies the exact runner006/current007 parser previously qualified by attached run35394332578. It does not exempt missing named objects: the absent global alternative requires exact logged threaded-WMO frontend source/object joins. `make()` receives the fresh owned `receipts/swift-output-maps` directory, and its retained map bytes are included in the existing receipts upload and final drift verification. All product/benchmark patches, full composed source manifests, counter validator, configuration, guard, workflow, flags, deadlines and workload are unchanged. No new supervision or compiler-proof framework is introduced.

## Narrow source-inventory successor004

Hosted003 run35402506292 stopped after exact SDK commit/tree authentication, before any Core fetch, resolve, build or workload. Its global `.swiftpm` path exclusion dropped the committed Examples/NotesApp/.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata, although the complete252-file expected SDK manifest correctly included it. The exact local Git-materialized source reproduces the old251-vs252 rejection.

This successor excludes only the root `.swiftpm` bookkeeping directory, preserving the existing root metadata boundary and narrowing the excluded set. Every nested path, including the committed example workspace and any unexpected nested metadata, now participates in exact inventory/hash checks. Exact252-file SDK and900-file Core base images pass; exact252/905 composed images pass. Missing and unexpected nested paths reject, as do changed bytes and symlinks. All native/product/benchmark sources, manifests, graphs, counters, guard, build parser, flags, workloads and resource/time limits remain unchanged. The reviewed activation workflow is reused with only its two exact-seal fallback literals replaced by the new packet seal. No native or network activity ran during preparation.

## Narrow declared-edit-link successor005

Hosted004 run35403098518 authenticated SDK/Core bases, applied the unchanged overlays, resolved the exact graph and completed the declared Core edit, then rejected a source symlink before compilation. Both primary and final source checks report the same generic error; the artifact contains no link-path/readlink inventory. Its actual offending link therefore remains unproven. All338 artifact members and154 successful command receipts/logs/cleaned process groups were independently verified. No build or mechanism workload ran.

An existing local materialized SDK at `/Users/jason/localdev/Lattice-testwt` independently establishes the SwiftPM `Packages/LatticeCore` layout: an absolute directory symlink to its Core owner, joined to the retained workspace edited path and LatticeCore subpath. `checks/PRIOR-MATERIALIZED-EDIT.json` retains this scoped evidence. This is prior-layout evidence, not reconstruction of the hosted filesystem.

The sole new exception is the exact SDK-relative `Packages/LatticeCore` link **after** the graph has authenticated the one Core edit. Raw readlink must be the exact absolute owned sibling `Core` path; target and SDK must be real canonical directories, and strict resolution must equal that target. Workspace edited path/subpath and graph path/revision are checked. Core's own complete905-file inventory remains independently authenticated. The edit link cannot overlap a committed SDK path. Missing links, relative spellings, wrong/dangling targets, ordinary replacement directories, symlinked Core/Packages parents, other links, root metadata links and nested source links reject. No entire Packages subtree or general symlink category is ignored. Initial/base source checks admit no links.

The source walk explicitly does not follow directory links. Root `.swiftpm` regular bookkeeping remains the existing inventory exclusion; nested `.swiftpm` source stays exact. Rejections identify relative paths and raw link targets, including observed links when the declared edit link is missing or wrong. Each successful full source verification now retains a small SOURCE-CHECK receipt recording whether graph authentication has occurred and the admitted link. Existing receipt upload retains these records. All product, counter, workload, expected source manifests,34-pin graph, build parser, guard, flags and resource/time bounds are unchanged. The workflow changes only its two admitted-seal fallback literals. No native/network execution or active checkout mutation occurred during preparation.

## Cold Swift attribution successor006

This packet extends the accepted005 mechanism experiment (run35404177828) with the eight source-only postimages from cold-swift-attribution-001. The same one-image local then attached workload now records four fixed thread-local cold-page brackets: native query, native hydration, Swift backend boxing and model mapping/registry work. Sidecar schema2 adds those four raw elapsed values, row counts, route and completion/error facts. The original scalar pool/execution/value oracles remain required, and the cold validator rejects missing/extra work, invalid route, incomplete recording or phase sums exceeding the unchanged enclosing elapsed interval. Outer residual is descriptive; no quantiles, cross-graph subtraction, speedup or2x claim is authorized.

The two SDK implementation paths are explicit changedPaths and complete-manifest entries. No translation unit or source file is added beyond the existing005 graph (905Core/252SDK). The exact qualifier, guard, build-proof parser, reporter, base manifest, prototype patch, frozen benchmark and diagnostic flags/bounds remain unchanged. Compiler/map/source/binary joins still apply to the actual image. Recorder/compiler compatibility and runtime sequence remain unqualified until parent-approved execution.

The inherited test_mechanism.py is intentionally byte-identical005 and contains its schema1 synthetic fixture. The source-only assembly checks supply the reviewed schema2 fixture in memory to its unchanged five assertions, alongside the eight cold-validator checks and eleven existing qualifier checks. This preparation check is not part of the hosted runtime command and is not a new supervisor. All original005 artifacts and failed predecessor runs remain retained.
