# Attached correctness runner 006

This source-only successor preserves the exact SDK d18f804/Core f8f72e9 graph, product correction, two-case overlay, Release commands, complete pins, all limits, both-builds-before-tests ordering and expected original-red/corrected-green oracles from 005. Root controls installation and execution. This is not benchmark or release qualification.

Actual run 35391067485 completed its original Release build successfully (1101.319 s; clean process-group teardown) then failed in the compiler proof at build_proof.py:99 while checking an output-map object. Neither the corrected build nor either test arm ran. The remote map and offending path were not retained. `RUNNER005-DIAGNOSIS.json` preserves the authenticated failure and the limit on historical attribution.

The exact Swift 6.3.3 log records threaded WMO drivers and frontends with 57 SDK and 90 test source/object outputs. An unused empty-key object alternative is a hypothesis for the old failure, not a recovered map fact. The correction accepts only an absent empty-key map object, only under threaded WMO, and only after one exact frontend joins every named source and object, toolchain, target, SDK, optimization and testability. Every named object must exist, be owned, hashed and transitively linked. Other missing or unexplained inputs fail. The successful proof records the joined frontend and absent alternative; final verification rejects map/copy/object/absence drift.

The original raw maps (at most 4 MiB per module) are copied under each arm’s receipt directory before parsing/validation and uploaded by the existing receipt path. Failures now identify module, driver line, map, key and object where applicable. Ephemeral frontend supplementary maps and derived linker temporary input bytes remain unverified; they are not used as provenance edges.

The original 29 Python checks plus 12 new map/join checks passed under the unchanged guard implementation with a 60-second Python command cap. New checks use actual retained driver/frontend command text and explicitly synthetic maps/files; they do not reconstruct the missing remote maps or prove a native build. The post-command summary-script TypeError is retained separately and corrected offline without rerunning tests.

Install all sealed files to Scripts/attached-baseline-correctness and update only the workflow packet seal to 006. Do not alter source graph, commands, clocks, limits or test assertions. Root must review this packet before dispatch.
