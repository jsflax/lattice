# Corrected attached baseline: selector preflight and four legacy prerequisites

Source-only successor009 to preserved runner008. Actual runner007 is terminal and separately assessed; its retained version log proves that008 would reject the numeric swift-driver prefix before any fixture command. Installation/execution remains subject to root admission. Run35394332578 independently reproduced the exact original display failure and passed both corrected focused cases, including physical postimages. Its result/assessment/seal are immutable inputs here; this runner never reruns or relabels the original arm.

Build **one fresh corrected** SDKd18f804/Coref8f72e graph in its own localdev root. The only product change remains product.patch2b4827d6 (TableResults attached registry bypass). Keep all34 complete pins and the identical opt-in focused test overlay from006 (compiled, disabled and unselected here). Add only owned-log.patch to BaseTest: require the runner's explicit owned log directory/path, refuse external/missing paths or open failure, and retain the existing FILE lifetime. All three source files containing the four selected tests are byte-identical to d18 and verified at every source gate; assertions, waits, concurrency, transactions and fixtures are unchanged.

Select exactly:

- LiveResultsIdentityTests/t7_forEachDiff_noDeleteInsertForSurvivors_fileDB()
- LiveResultsIdentityTests/t7_reuse_liveWeakRefIsSameObject_deadRefHydratesFresh()
- LiveResultsKeysetTests/deepScroll_jumpPaysOneOffsetStatement_thenKeyset()
- AttachTests/attach_updateInSynced_throughQueryLattice()

Require discovery of all four exact IDs, ordinary exit0, exactly four XML cases and matching starts/passes, no skip/error/failure/global issue/signal. No expected-red admission exists in this successor. Do not add unsupported concurrency flags; existing suite traits and Swift Testing defaults remain. These four bodies contain no conditional successful early-return path.

The original guard and compiler-proof helper are byte-identical006. Keep5400s Release build /180s tests /18000s total with600s finalization reserve,-j2,12GiB free,30GiB packet,512MiB command log,0.5s sampling and whole-owned-group cleanup. Retain all source/34-pin/compiler/object/link/binary proofs and final drift/resource/deadline checks. The unchanged graph and verify_build functions continue to bind the original in-memory proof and receipt hashes. Build with -c release -Xswiftc -enable-testing; use --skip-build only after authenticated success. All paths and logs are owned; the legacy fixture teardown is unchanged, without a new claim that every native handle was closed before inherited deletion.

Only actual successful runtime may set legacyChecksQualified/correctedBaselinePrerequisitesAccepted. Any final custody/resource/deadline/write failure clears them while retaining observations. benchmarkAdmissionAccepted, performanceQualified, fullContractQualified and originalSafetyAccepted remainfalse even then: parent still reviews benchmark input/host/workload admission. The predecessor's focused pass stays explicitly under predecessor, not presented as newly executed here.

The inherited007 patch/source qualification is retained; product/test/logger overlay bytes remain unchanged. This successor contains 14 pure selector tests and reruns the unchanged 10 legacy classifier tests, plus the new classifier against retained actual Swift6.4 three/zero/one XML/body logs. This is not Swift6.3.3 runtime proof. No Swift parse/typecheck/build/test/network occurred during preparation. A separate receipt records calling the real validate_packet(new seal) after freeze. Root owns review, install and remote execution.


## Same tiny selector fixture first

Before any historical SDK/Core fetch, resolution or build, run the exact two source files from `swift-testing-filter-probe-001` (no dependencies, three Swift Testing bodies). Require the actual configured Apple Swift6.3.3 version. The observed local continuation002 passed on Swift6.4 only; it does not qualify the hosted toolchain. No fixture source or body implementation is rewritten here. The fixture inventory must contain exactly the two declared files with no symlinks; only those two files are copied into the owned build package.

Use the existing GuardedRunner and environment, with a separate owned selector-probe package/scratch/cache/config/security/tmp/module-cache tree. The fixture has a60s build and four15s commands: discovery, unfiltered3-case baseline, old end-anchor zero observation, and corrected exact module-dot selector yielding only the chosen case. All time counts inside the unchanged18000s total/600s reserve; no native-command or overall bound increases. The60s/15s cheap limits come from the original001 command plan.

Require three exact discovery IDs. Join XML cases, direct `Test.current.id` body records, and ordinary start/pass/summary events. Recognize singular and plural summary forms. The old selector must show zero cases/bodies and the no-matching warning or zero summary; that is an observation, never test qualification. The corrected selector must produce exactly the same chosen body ID observed in the baseline, one exact XML case and1/1 summary; both distractors must be absent. Any mismatch stops before the historical SDK graph. All command receipts still require clean ordinary exit0, reaped leader and absent owned group.

Capture source and the one freshly built tiny test image hash before discovery/body execution; verify them before and after every arm and at finalization. Retain all raw XML/logs and individual classification receipts under the existing receipts upload. `SELECTOR-PROBE.json` is separate from the four real legacy results; later overall failure clears top-level selector acceptance while preserving its raw receipt. This is postbuild binary custody, not a new full compiler-object proof for the tiny fixture. Unexpected test-image layouts fail with a bounded candidate path receipt.

The only legacy runtime filter change is its final `$` → `(?:/|$)`, allowing Swift Testing's observed optional source-location suffix. Exact discovery4, XML4, starts4, passes4 and identities stay unchanged in the byte-identical legacy classifier. The four legacy body files, original180s test deadline and default concurrency are untouched. No benchmark, performance, broader suite or release acceptance follows from the tiny fixture.

## Narrow009 correction

The sole runtime delta from008 is the version-line gate: accept an optional literal `swift-driver version: ` followed by dot-separated numeric components and one space before exact `Apple Swift version 6.3.3`. The retained007 log starts `swift-driver version: 1.148.6 Apple Swift version 6.3.3`; its exact128-byte output is preserved as `selector-observed-swift-version.log` and bound to SHA256 `5fa4b669e41b23b9ad0760253d062493487f93cf5d8ad8c44f0d4fd635137499`. No warnings are stripped, no arbitrary prefix is accepted, and no newer/different Swift version is admitted.

The real probe function is exercised with both the original plain version line and the actual retained hosted log using fake commands only, including success/failure environment restoration. Wrong versions, malformed prefixes and snapshot suffixes must reject before directory setup or any command. All Swift sources, native argv/timeouts, graph inputs, product/logger overlays, legacy classifiers, compiler proof, guard and workflow behavior remain unchanged from008. The workflow differs only in the new packet-seal value. This preparation does not run Swift or establish hosted selector qualification.
