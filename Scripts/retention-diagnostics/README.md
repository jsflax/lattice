# Retention phase coverage follow-up

This preparation changes only diagnostic capture and interpretation of one traced B sample. Core remains exactly 7b11c282800d2bd6478538edb4e87de81953d315 / tree18b482a47df9413e151887ef77a9dc5abb6be041. The original uninstrumented five-triplet run35365051287 and failed diagnostic run35368549000 remain immutable. This workflow does not repeat the four floor-race baseline cases. Their safety failure remains independent and unresolved.

The shared build contract is unchanged: fresh uniform O3/g0/DNDEBUG Core and both previously reviewed probe targets, exact translation-unit/object/archive/map/binary verification. The floor executable is built and verified to preserve that contract, but no floor command is invoked. Only the changed phase sample runs. Source readiness remains false pending root review; no native compilation or execution has been performed for this correction.

Capture begins after the parent command wait. Installation entry/return, the original tick start/end and removal entry/return delimit the observation. Background workers remain active. Every recorded interval carries only whether its start/end callback ran on the synchronous tick thread; no thread ID, SQL body, bound value, payload, path or pointer is emitted. The existing 256-record / 32-active-statement / 16KiB recorder limits remain. Unknown statements add a prefix fingerprint of at most512 bytes and a truncation flag; collisions are possible and no fingerprint can turn an unknown statement into a qualified phase.

New exact classifications cover the lazy observer-column ALTER, cross-process vector-catalog lookup and local commit audit-head lookup. No migration is moved out of the tick. Before/after/straddling records remain in the analysis. Wall phase totals and writer overlaps are clipped to the tick; full statement spans and PROFILE values are separately retained. Required maintenance phases must actually start on the tick thread. Every unknown record (even outside the tick), overflow, unfinished statement, changed-thread statement, callback-accounting error or installation-window violation keeps qualification false. Historical v1 input remains explicitly unqualified because it lacks capture boundaries and thread identity.

All guards and workload details remain unchanged: 12.5GiB free,512MiB packet,32MiB logs,-j1,1200-second compile,120-second sample,1000 foreground writes and10k history rows. No workload sleeps, deadlines, assertions, product SQL, worker cancellation or retention bounds change. This trace is descriptive evidence, not a performance comparison or product acceptance. The original PROFILE/result-code and pre-STMT prepare/connection-lock blind spots remain.
