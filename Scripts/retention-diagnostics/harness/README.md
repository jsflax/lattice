# One-shot retention phase diagnostic (preparation only)

Exact Core `7b11c282800d2bd6478538edb4e87de81953d315`, tree `18b482a47df9413e151887ef77a9dc5abb6be041`, is exported without product changes. `SOURCE-READY.json` remains false; root review/explicit activation is required before the prepared remote workflow can run. This is a separate packet. Uninstrumented run 35365051287 and its five triplets remain immutable.

The seed and writer bodies are byte-identical to the reviewed foreground harness: 10,000 old audit entries plus ten fresh model rows, 1,000 foreground updates with a control-pipe pause after 200, 256-byte foreground bodies, 5-second database busy timeout, 600-second retention, deterministic backdated watermark. The original supervisor releases maintenance, then releases the writer when it observes tickEntering. It still validates all 1,000 writes, current model values, exact audit survivor range/count, sequence, restored sync flag, tick overlap and closed-file checkpoints. This packet performs exactly one instrumented B sample, with no A/A/B rerun or automatic retry. The successful original claim/retry/floor/late-remote/held-reader controls are retained separately; this sample does not rerun them or claim to extend them.

`SQLITE_TRACE_STMT | SQLITE_TRACE_PROFILE` is installed only on the owned maintenance connection after automatic retention stops and the parent command wait returns. Native Core opens it FULLMUTEX. Background notifier work can still use the same connection; it is neither stopped nor assumed to be maintenance work. Hook registration/removal serialize on that connection; the recorder is read only after removal. Stack storage is capped at 256 intervals plus 32 active statement identities (compile-time <=16KiB). All counters saturate at UInt64 max. Each callback reads a monotonic clock, compares the current thread against an immutable tick-thread identity, checks fixed SQL templates to choose an enum, and writes scalar slots. Unknown SQL additionally gets a non-cryptographic fingerprint over at most512 prefix bytes, plus byte count/truncation metadata; the fingerprint never qualifies an unknown statement. There is no allocation, output, extra lock, SQL execution, payload/value copying, expanded SQL or application callback in the hook. Trigger-comment notifications are counted but do not create another interval for the same prepared statement. RAII removes the trace before its recorder can die on an exceptional path.

Only after trace removal does the child emit one bounded maintenancePhases record. It includes installation/removal boundaries and, per interval, phase, ordinal, monotonic begin/end, completion flag, tick-thread start/end booleans and SQLite's approximate PROFILE duration; no SQL text, values, paths or pointers are emitted by this new record. The existing identity/fixture receipts remain unchanged. Excess records, active-slot exhaustion, unmatched profiles and incomplete intervals are explicit and cause trace qualification to fail after preserving analysis. Unknown SQL is labeled other and prevents phase qualification, while preserving the raw trace. Ordinary time outside STMT/PROFILE is separately unattributed and is not mistaken for a dropped record. Classification is only for the fixed maintenance SQL templates; the first-comment rule is not a general SQL parser. The existing 1MiB line / 32MiB child-output bounds remain; a full phase record is capped structurally to 256 fixed scalar objects.

Phases include claim, watermark max/store/bound, floor schema/migration/read, background vector-catalog lookup, local audit-head lookup, cursor read, BEGIN, audit and sync-state DELETE, receipt cleanup, sync disable/restore, COMMIT/ROLLBACK and claim cleanup. Trace starts at statement execution, after prepare/native connection-lock acquisition; earlier work stays unattributed. PROFILE does not expose a result code or exact write-lock boundaries. The reported explicit transaction brackets are BEGIN-start→COMMIT-end and BEGIN-end→COMMIT-end. Successful persisted outcomes come from the unchanged fixture validator, not PROFILE. Nested statements remain inclusive; the summarizer additionally computes union duration to avoid double counting. It retains pre/post/straddling records and full SQL spans but clips wall totals and writer overlaps to the tick; required phases must be tick-thread records. Historical v1 captures remain unqualified because installation/thread metadata is absent. Longest ten writer intervals are joined against exact captured phase intervals on the same Linux host's monotonic clock. Temporal overlap is evidence of simultaneity, not proof of a particular lock wait.

The instrumented sample is not appended to the uninstrumented timing distribution and has no numeric latency gate. Any chunking or product change waits for interpretation of this trace and independent floor-race results.

Build/guard policy remains uniform O3, DNDEBUG, g0, no LTO, compiler -j1, 1200-second compile child, 120-second sample child (115-second supervisor), 12.5GiB free floor, 512MiB packet, 32MiB logs. No full SDK build, local native execution, dependency mutation or installed workflow change was performed to author this packet. The source export, exact compiler-object/archive/map evidence, runtime SQLite identity and process-group cleanup are the inherited reviewed proof mechanisms.

Nineteen pure-Python analyzer cases cover interval union, overflow/missing evidence, unfinished intervals, unknown SQL class, malformed phase/bounds, clock ordering, foreground cardinality, rollback, pre/post/straddling intervals, thread substitution, counter accounting, migration cost and preservation of historical failure. They are helper validation only. Native callback behavior and the actual diagnostic sample remain unexecuted until the reviewed remote run.
