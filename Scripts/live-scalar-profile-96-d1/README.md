# Held hosted live-scalar diagnostic — SDK96 / Core d1

This is an executable **source-only, unallocated** adaptation for one instrumented B build/run on `macos-26`. The workflow job is explicitly disabled. No compiler, resolver, preprocessor, SQLite, native/runtime, browser, network or dispatch has run during preparation. There is no runtime root. The existing negative frozen benchmark remains unchanged; no A/A2/baseline workload is built or run here and no target or physical-host acceptance can be emitted.

The exact bases are SDK `96a5bf0256f81691a92d98cb8fe24b54e00de811` / tree `466e724abeea3e01f0af8c5d7adde729628e4115` and Core `d1e06f4b74ffc76410a9e59f062117c160e191b6` / tree `5280333ecc94b8e7339010c9c8e70a30019f86b9`. Both historical full correctness gates are already accepted. Ten small copied evidence files bind the SDK parent plus macOS/Linux children, Core parent plus native/C ABI children, and the reviewed diagnostic source/patch identities. `profile_binding.prerequisites` validates their exact byte hashes, parent-child hashes and source identities before output creation. It grants no stock binary, profile-build or runtime credit and reruns no historical test.

## Source and execution delta

`guarded_runner.py`, `build_proof.py`, `perf_refinement_report.py` and `frozen_binding.py` are byte-identical copies of the accepted frozen packet's existing helpers. The original complete-source and strict command audit functions are reused directly. Source acquisition remains fresh exact Git checkout and all 34 committed SwiftPM pins, followed by the sole explicit local Core edit. The graph checker has one reviewed policy extension: the Core checkout may have exactly the three diagnostic edits, only if its entire postimage manifest matches; every other dependency must remain clean at its committed pin.

`run-profile.py` removes baseline/A/A2 acquisition/build/measurement and adds only the two accepted diagnostic patches, postimage manifests, profile proof and sidecar analysis. Pristine 253-file SDK / 901-file Core bytes are authenticated before patching; afterward all 253 SDK / 903 Core files are checked, including two new headers. Every changed byte is in the sealed five-path source overlay. Package manifests/locks, fixture/sample bodies, normal product defaults and the other sources are unchanged. Build flags explicitly select Release, selected-batch writes, Swift profiling and C/C++ importer profiling. An owned Core include path exposes the test-only C seam; no public feature is activated by default.

The one filtered `--skip-build` command performs the unchanged local→attached, 5 warmup + 100 measured sequence, then the separate 8-per-variant off/on calibration: **210 canonical instrumented samples plus 16 calibration samples**. The selected-batch source, all eight SQL budgets, checksums, identity/freshness, original SQLite validation and sample order are checked. Calibration is excluded from `result.json` and the frozen comparison; it measures only incremental native-counter overhead, because phase clocks and diagnostic branches remain in counter-off mode. Swift/bridge conversion and native per-category CPU remain unmeasured.

No new process supervisor is introduced. `StrictRunner` only adds the existing strict receipt/log check immediately after every successful `GuardedRunner.run`, including helper-generated Git commands. The same 12 GiB free floor, 30 GiB packet ceiling, 512 MiB per-log bound, j2 build argument, 5,400-second build, 1,200-second measurement, 18,000-second overall and 600-second final reserve are retained. The original 60-second settling policy is descriptive and does not establish host idleness. SwiftPM's `-j2` argument does not claim at most two total OS/compiler threads; original Release WMO threading remains recorded by the actual compiler proof.

First failure stops work with no retry, patch repair, deadline widening, count reduction or fallback. Finalization separately attempts packet/admission/prerequisite checks, source custody, object/header/tool/binary proof, each command audit, final resource measurement and the absolute deadline. A failed command cannot short-circuit later evidence; primary failure and every evidence error remain distinct. The resource guard still describes owned process groups/reaped leaders, not arbitrary escaped descendants.

## Actual profiling proof

The frozen build proof still authenticates the actual Core/SDK/test source actions, named Swift output maps, real objects, canonical link input lists, linked archives and test binary/bundle. `profile_proof.py` supplements it with explicit native profile definitions, selected Swift profile/batch definitions, C importer definitions, response expansion, conflict/undef/forced-include refusal, actual tool hashes, and exact five postimages. The database object and harness object must be reachable from the just-built test binary. No prior stock object or binary fingerprint is reused.

The actual `db.cpp` action must request exactly one `-MD` and the bound `-MF` dependency file must name both new native/profile headers and `db.cpp`; the actual global `LatticeTests.d` referenced by its output map must name the new C header, SDK C shim and harness. The bounded dependency parser reuses the existing continuation/shlex method, accepts one make rule with multiple targets, and binds actual bytes under owned source/scratch plus observed Xcode SDK/toolchain roots. Depfile copies and hashes of all consumed files are retained and rechecked. Actual stable link lists, output maps and owned response-file bytes are also copied into receipts with source/retained hash joins; ephemeral derived-child link lists keep the frozen proof's explicit unverified-byte limitation. A future compiler emitting repeated rules or omitting textual imported headers is a first-failure proof refusal, not permission to fall back to header presence. Actual Release `.d` shape/import/link correctness remains unqualified.

The artifact template retains the five instrumented source files, complete receipts/sidecars/fixtures, actual Core/bridge/SDK/test build directories, test bundle and any archives. Actual host toolchain bytes/paths are freshly observed; no local Xcode fingerprint is credited remotely. System toolchain/header content hashes are retained as evidence, not copied into the artifact.

## Admission and exact future entry

`profile-hosted.yml` remains disabled (`if: ${{ false }}`). Before installation/enabling/dispatch, root must review this packet, the independent review, the enabled workflow delta/new seal and a fresh owner resource allocation. No allocation is requested by these files. The owner receipt schema is in `ADMISSION-TEMPLATE.json`; its `approved:false`, missing allocation and placeholder seal/hash are intentionally rejected. The CLI also requires an exact GitHub workflow SHA, fresh owned root/temporary directory and no earlier receipts or profile outputs.

When separately authorized, install this sealed directory as `Scripts/live-scalar-profile-96-d1` in the reviewed tools checkout. The only entry is:

```
python3 -B "$TASK_ROOT/tools/Scripts/live-scalar-profile-96-d1/run-profile.py" \
  --root "$TASK_ROOT" --seal-sha256 "$PACKET_SEAL_SHA256" \
  --admission-json "$TASK_ROOT/ADMISSION.json" --admission-sha256 "$ROOT_ADMISSION_SHA256"
```

The concrete resolve/build/run argv and environment are in `run-profile.py`; all paths are owned under a new `~/localdev/lattice-live-scalar-profile-<run>-<attempt>`. Version/SDK/developer-path receipts, same-boot host samples, source/graph proofs, build/profile proof and binary hashes are joined to the new build identity and the profile manifest. `PROFILE-ANALYSIS.json` only describes instrumented sample CPU/native timing and four off/on observations per mode. It cannot substitute for the canonical uninstrumented A/A2/B p95 goal, establish host causation, or credit later eee/917 source.

Pure verification uses inert generated text/object fixtures under packet `pure-tmp`, never a compiler or database. The retained log records the exact test count. Unsupported command/dependency shapes are refused. A source-ready seal is preparation evidence, not native, host, runtime or performance acceptance.

## Proof correction 002

The sealed001 source review found that existing inert object/depfile bytes could receive credit without object-producing commands or a dependency-generation request. This successor changes only the supplemental parser and adds 21 pure controls. Selected Swift drivers require `-c`; each selected module requires exactly one logged compile frontend after its driver, from the same compiler directory, with unique exact consumed source and object operands. Module-only frontend output cannot replace it. Syntax/typecheck/preprocess/assembly/IR/dependency-only contradictions are refused. The native database action requires one explicit full `-MD` mode, rejecting missing/repeated flags and unsupported `-MMD`. These are source proof corrections, not findings about prior real build correctness.

The runner, frozen build proof, guard, config, all resource limits, workflow, analyzer, source patches/manifests and original33 proof tests are byte-identical001. The original14 runner tests only update the asserted absent successor runtime path. All68 pure checks pass (47 prior +21 new). The initial invocation accidentally addressed the new test file relative to the wrong directory and reported a missing test module; its setup-only log is retained. No compiler or product ran. `PROOF-DELTA.patch` and `PROOF-CORRECTION.json` record the complete bounded change and bind the preserved001 source/review identities. The workflow remains disabled and no runtime/admission exists.
