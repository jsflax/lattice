This packet attempts to prove whether hosted Swift 6.3.3/macOS can retain a useful OS report for the tiny known SIGSEGV control. It contains no reachable SDK/Core checkout, package resolution, SDK compilation, test-suite invocation, network action, debugger, permission change, or fallback. Diagnostic source002, control003, and their failed runs35387156738/35389035775 stay unchanged. The existing full SDK diagnostic is not yet switched to this mechanism.

Run35389035775 completed the direct SIGSEGV with clean cleanup and found/read one fresh `CrashControl-2026-09-18-200030.ips`, but discarded it because the literal executable path bytes were absent. No raw report survived. This does not establish JSON slash escaping, OS path redaction, or a different path as the cause. Successor004 decodes bounded JSON before path comparison and preserves narrowly attributed raw diagnostic candidates so the next rejection remains inspectable.

`control_probe.py` copies the exact existing `CrashControl.swift`, records the exact `swift --version`, compiles with the original `-Onone -g -module-name SDK46CrashControl` arguments, and launches the binary directly through the unchanged copied supervisor. Native caps are 30 seconds for version discovery, 60 for compile and 10 for execution. The actual signal receipt must show `-11`, clean leader/group cleanup, no intervention, timeout or evidence error. A raw command receipt still says failure for that signal; only the explicitly expected-control classifier can accept it. The refused `SWIFT_BACKTRACE` setting remains recorded; an OS report, rather than runtime stderr, must supply the frame.

The control-only overall ceiling is 240 seconds with 30 seconds reserved for finalization. This contains the native caps, a complete newly approved 60-second report-arrival window, and bounded source/receipt overhead. Before entering that window, the runner requires all 60 seconds remaining in its work budget. The original supervisor retains the 12 GiB free-space floor, 30 GiB packet ceiling, 512 MiB log limit, signal handling and owned-process cleanup. No original SDK test deadline is changed.

`control_reports.py` retains the hardened collector and one 60-second scan. The actual clean SIGSEGV receipt now supplies its PID and launch/exit interval before collection starts. A report is copied only after bounded JSON decoding finds that exact integer PID, exact `procName` equal to the owned executable basename, `procLaunch` inside the measured launch/exit window, and `captureTime` inside the launch/scan window. JSON duplicate keys and ambiguous documents reject. The candidate's decoded path and equality result are bounded metadata; missing, redacted or different paths may be retained only under the PID/name/time conjunction and never qualify full admission. The raw bytes/hash are preserved, without rewriting the report.

It reads the same user/system DiagnosticReports directories. Regular-file/no-follow/stable-identity checks, 8 MiB per file, aggregate 8 files/16 MiB, 64 eligible read attempts, 4,096 entries per directory pass, bounded metadata, reserved custody before writing, stop-on-output-failure and final rehash are unchanged. Limits are aggregate for this control-only job; a later combined control+SDK collector must not reset them. Files that cannot establish the PID/name/time conjunction are not copied, and their bounded rejection reasons remain explicit.

Full admission still requires exact decoded executable path/PID, capture time inside the launch-to-scan window, consistent process-launch time when present, SIGSEGV with EXC_BAD_ACCESS/EXC_CRASH, one explicitly triggered thread matching `faultingThread`, and a symbolized `latticeSDK46DiagnosticCrashControl()` frame on that thread. The frame's image index must point to the exact control path and include a UUID. Modern `.ips` JSON supports one optional metadata document before its body. Timestamp syntax accepts ISO offsets and a narrowly recognized date/time with a separated numeric timezone; this does not change the time bounds. Whole-second serialization permits only floor/ceil of measured endpoints. A source string, another thread's frame, a different image, address-only frames or malformed/ambiguous data reject.

The report body/schema and function rendering remain **prospective** until actual hosted bytes are retained. Legacy non-JSON `.crash` text cannot establish the new identity conjunction and is rejected without copying. If a matching PID/name/time report lacks the exact path or symbol, the job fails while retaining those raw owned candidate bytes and bounded rejection reasons. No `atos`, `dwarfdump`, dSYM generation, heuristic address attribution or extra symbolication step is presumed. A separately reviewed change can use actual report/binary identity if needed; there is no automatic retry.

Acceptance is authoritative only in final `RESULT.json` with `success=true`, `controlCaptureAccepted=true`, no errors/signals and final source/binary/report hashes intact. A preliminary `CONTROL-ADMISSION.json` alone never admits SDK work. All SDK/full-suite/release/cause flags remain false even on a successful control. Missing reports, unsupported symbolication, failed final custody or final result write force capture acceptance false.

Root may run, remotely and only after review:

```text
python3 -B control_probe.py --root <fresh absolute path under ~/localdev> --source-seal-sha256 <reviewed SOURCE-SEAL.json SHA256>
```

No native process was launched during preparation. The focused Python checks include JSON-escaped slashes, retained redacted/missing paths with strict admission failure, wrong PID/name/time rejection, duplicate-key rejection, timestamp syntax, and actual raw-byte/hash custody of a retained rejected candidate. Counts/results are sealed separately in PREPARATION.json. These checks do not prove hosted report-body compatibility. Later SDK integration must preserve exact graph identity, original full-suite selection/default concurrency, 1800-second cap and every assertion; this packet does not perform that integration.
