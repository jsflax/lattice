This preparation-only successor proves only the tiny direct SIGSEGV control's OS-report capture. It contains no SDK/Core checkout, network, package resolution, SDK build/test, debugger, security/privilege change or fallback. Original source002, controls003/004 and all failed receipts remain unchanged.

Actual hosted attempt 35390339258 cleanly exited -11 and retained its fresh 7,560-byte report (SHA256 0c86c5e55113d30f421079a1d7f7f0759c1f5e92a55157efb6ecfa8ccd9ad4dd). The report has procPath and the named crashed frame's image path both exactly `/Users/USER/*/CrashControl`; image UUID is aaa9e574-35c0-4ba5-81e6-8aa712cffe8b. This is now observed path redaction, not an explanation inferred from missing bytes. The old binary hash was retained, but its UUID was not. This successor does not retroactively accept that run or derive an expected UUID from its report.

Before launching the newly compiled owned control, `macho_identity.py` reads its regular, no-follow, stable file (maximum 8 MiB) and parses the thin little-endian arm64 MH_EXECUTE header and its unique nonzero LC_UUID. It rejects fat/other-endian/other-architecture/non-executable input, truncated or malformed command spans, more than 256 commands or 64 KiB of commands, duplicate/missing UUID and replacement during the read. It does not execute a native tool, parse a dSYM or extract symbols. The 32-byte header, LC_UUID 0x1b/24-byte command and architecture constants are recorded from local Apple SDK headers in SDK-CONTRACT.json. These bounds are additional fail-closed limits, not increases to the existing packet/custody limits.

The exact full executable SHA256 must match both the previously admitted compiled binary hash and the Mach-O read. `CONTROL-BINARY-IDENTITY.json` is written before launch and its in-memory identity is retained. Finalization re-reads and compares the full binary identity and that receipt against the originally admitted value, alongside the unchanged source/report/command/resource/deadline checks. Neither report UUID nor mutable receipt may replace this anchor.

Strict admission accepts only (a) the exact owned executable path or (b) the single literal observed string `/Users/USER/*/CrashControl`, with the report image path equal to that same representation. The asterisk is not interpreted as a glob; prefixes, other redactions, arbitrary paths and mixed exact/redacted paths fail. Both routes also require the named crashed frame's image UUID to equal the independently captured binary UUID. The report records the original reported path, owned executable path and explicit identity mode separately. It does not claim the redacted path equals the actual path.

Exact integer PID, `procName=CrashControl`, launch time, capture time, SIGSEGV/EXC_BAD_ACCESS-or-EXC_CRASH, one triggered thread matching faultingThread, and the exact symbolized `latticeSDK46DiagnosticCrashControl()` frame remain required. Launch time/name are now explicit in full admission as well as the existing custody gate. UUID comparisons permit case representation only. Time windows retain the existing floor/ceil allowance for whole-second serialization, never a broader age window. Unknown formats and unsymbolized frames fail. No added symbolication step is presumed.

The exact control Swift source, `swiftc -Onone -g -module-name SDK46CrashControl` arguments, required Swift 6.3.3/arm64 Darwin environment and supervisor are unchanged. Native caps remain version 30s / compile 60s / direct execution 10s; overall 240s with 30s finalization reserve. The existing 60s report-arrival scan requires all 60s in the remaining work budget. Free-space floor 12 GiB, packet ceiling 30 GiB, log limit 512 MiB and owned group cleanup are unchanged. The runtime backtrace refusal remains recorded and is not treated as report proof.

`control_reports.py` is byte-identical to004: bounded PID/name/launch/capture custody retains raw candidates even when strict path/UUID/symbol admission rejects, using the same two DiagnosticReports directories. Aggregate limits remain 8 files / 16 MiB / 64 eligible reads, 8 MiB per report, 4096 directory entries per pass, one 60s window, safe-open/identity checks, reserved output custody and bounded error metadata. This packet adds no scan or fallback. Later SDK/control combined collection must not reset these budgets.

Only final RESULT success with controlCaptureAccepted, no primary/evidence/signal errors and all custody intact admits this control mechanism. Preliminary admission alone is insufficient. SDK/full-suite/release/cause flags remain false even on control success. A result-write failure clears capture acceptance. Root reviews and remotely runs:

```text
python3 -B control_probe.py --root <fresh absolute path under ~/localdev> --source-seal-sha256 <reviewed SOURCE-SEAL.json SHA256>
```

Preparation performs only bounded Python synthetic/parser/file checks; no control/native process or network is launched. Checks include exact/redacted UUID joins, mismatches, wrong image/name/time/frame, strict path representations, Mach-O bounds/architecture/UUID/duplicate/malformed commands, owned file reads, symlink refusal and replacement hashes. Actual hosted UUID extraction and runtime qualification remain pending. Later SDK integration must separately prove helper executable identity and preserve original full-suite selection/default concurrency/1800s cap; it is not part of005.
